angular.module('AdminDashBoard')
    .controller('ListController', function ($scope, $rootScope, $location) {
        $rootScope.PAGE = "all";

    }).controller('UserController', function ($scope, $rootScope, $location) {

    });
